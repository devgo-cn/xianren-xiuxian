# 《闲人修仙》横版推进战斗系统规范

> **修订说明（2026-09-15）**
> 本文档早期版本包含一整套"素材强制契约"：逐帧内容框对齐、统一素材合同
> `assets/sprite-manifest.json`、合同生成器、形态校正 `correct` 等。
> 这套东西在迭代中**被否决并已回退**——逐帧内容框缩放会让角色随帧拉伸（蜷缩帧被拉高），
> 效果比不校更差。相关章节已删除，配套的 manifest 与生成器也已移除。
>
> 现存内容 = **当前代码实际在遵守的规范**。素材部分降级为"现状记录"，**不再作为强制项**。

---

## 〇、设计立场

| 立场 | 说明 |
|---|---|
| 战斗区是**表演** | 敌我血量刻意设成"一刀秒/几刀秒"，追求节奏感而非数值深度 |
| 玩家不会真死 | `if (p.hp <= 0) p.hp = p.maxHp;`（满血复活）——不能让背景装饰打断主游戏 |
| 判定用**一维水平距离** | 本系统无纵向机动，不做像素包围盒碰撞 |
| 服务权威、客户端表现 | 客户端渲染与预测，结算由服务端负责 |
| 规范描述现状，不预设美术 | 素材怎么画、画多大，**不由本文档规定** |

---

## 一、系统边界

| 职责 | 归属 |
|---|---|
| 玩家推进、刷怪、攻击、受击、死亡消散 | 战斗系统（`index.html` 内战斗 IIFE） |
| 存档、境界、灵石、弹窗 UI | 主游戏（`game.js`） |
| 二者唯一耦合点 | `window.BattleAPI`（§5） |

**边界铁律**：主游戏**不得**直接读写战斗内部状态（`G` / `BC`），一律走 `BattleAPI`。

---

## 二、坐标系与几何规范

### 2.1 坐标定义

| 概念 | 定义 | 说明 |
|---|---|---|
| **世界坐标 `x`** | 角色在世界中的横坐标，只增不减 | 玩家持续向 `+x` 推进 |
| **摄像机 `camX`** | 视口左边缘对应的世界坐标 | `camX = player.x - stageW()*0.28` |
| **屏幕坐标 `sx`** | `sx = worldToScreen(x) = x - camX` | 唯一渲染入口 |
| **地面线 `floorY()`** | `CH * 0.82` | 所有角色的**脚底**落在此线上 |
| **垂直坐标 `y`** | 相对地面的偏移（`translate(sx, floorY())`） | **不要**用绝对 Y 定位角色 |

**铁律**：角色逻辑坐标只用**世界 X**；一切绘制必须经 `worldToScreen()`。

### 2.2 摄像机

```js
const targetCam = G.player.x - stageW()*0.28;      // 主角恒定在视口 28% 处
G.camX += (targetCam - G.camX) * Math.min(1, dt*6); // 阻尼跟随，不硬切
```

| 参数 | 值 | 含义 |
|---|---|---|
| 主角屏幕锚点 | `28%` | 主角永远显示在视口左侧 28% |
| 跟随阻尼 | `dt*6` | 掉帧时不"跳跃追帧"，对移动端低帧率至关重要 |

### 2.3 攻击范围（`atkRange`）

攻击范围是**世界坐标下的水平距离**，判定与移动都用它。

| 实体 | `atkRange` | 语义 |
|---|---|---|
| 玩家 | `75` | 剑锋可及的左右各 75 世界单位 |
| 妖卒 slime（近战） | `30` | 贴身才打得到 |
| 水灵 water（近战） | `35` | 略长，血更厚 |
| 宠物（默认） | `50` | `addPet` 可覆盖 |

> 占位怪妖将 / 妖弓已移除（无素材，只画椭圆）。

**判定公式**：

```js
// ① 玩家起手：距离 ≤ atkRange + 5
if (near && Math.abs(near.x - p.x) <= p.atkRange + 5) { /* 起手 */ }

// ② 敌人停步：停在玩家 atkRange 处（+2 死区防抖）
const stopX = p.x + e.atkRange;
if (e.x > stopX + 2) e.x = Math.max(stopX, e.x - e.speed*dt);

// ③ 玩家命中：距离 ≤ atkRange + 15（比"起手"宽 10，防走位抖动导致空刀）
if (Math.abs(p.attackTarget.x - p.x) <= p.atkRange + 15) { /* 结算 */ }

// ④ 索敌：视野远大于攻击范围
findNearestEnemy(p.x, p.atkRange + 200);
```

| 容差 | 值 | 作用 |
|---|---|---|
| 起手 `+5` | 5 | 略宽于 atkRange，避免边界抖动反复切状态 |
| 命中 `+15` | 15 | 起手到出招有动画延迟（帧 10 / 帧 26），角色可能已轻微错位 |
| 索敌 `+200` | 200 | 保证"看得见"屏幕边缘刚刷出的怪（宠物为 `+100`） |
| 停步 `+2` | 2 | 防止 `e.x == stopX` 处抖动 |

> `atkRange` 与 sprite 尺寸**完全解耦**：换素材、改画布高度都不会改变判定距离。
> 反过来，美术把武器画长了 30%，`atkRange` 就该同比调——否则刀尖碰到敌人却不掉血（§7.1）。

### 2.4 移动与推进

- 玩家**自动向 `+x` 推进**，速度 `BC.playerSpeed = 28`；攻击动画期间 `p.moving = 0`（站定出招）
- 敌人从屏幕右外侧 `G.camX + stageW() + 40` 生成，向 `-x` 走来
- 到达 `p.x + e.atkRange` 停步并攻击

---

## 三、时间与节拍

### 3.1 帧循环

```js
const dt = Math.min(0.05, realDt);   // dt 上限 0.05s：切后台回来不会"瞬移"
const sdt = dt * G.speedMult;        // 逻辑用 sdt；动画/特效用 dt
```

> **逻辑用 `sdt`，表现用 `dt`**：倍速只加快战斗节奏，不把动画也快进成鬼畜。

### 3.2 倍速（由身法技能驱动）

原来的固定 `double/triple` 概率表**已废弃**，改为两个身法技能在**击杀时独立 roll**（见 §4.5）：

| 技能 | 倍率 | Lv1（概率/时长/闪避） | Lv20（概率/时长/闪避） |
|---|---|---|---|
| 疾风步 `jifeng` | 2x | 5% / 4s / +4% | 65% / 8s / +25% |
| 缩地成寸 `suodi` | 3x | 0.5% / 2s / +8% | 12% / 4s / +40% |

- 两者**独立 roll，取高者**；已在加速中再触发则**刷新时长**
- 满级疾风步 65% × 8s，配合高频击杀 → **二倍速几近全程**；三倍速作为稀有惊喜
- 身法**不是属性光环**：只在生效那几秒加闪避，时效一到 `G.speedDodge` 归零，不写回面板
- `trySpeedSkill()` / `triggerSpeedSkill()` 保留作外部触发口

### 3.3 动画帧推进

- 动画 `24 fps`，`frameDur = 1/24`
- 走路用**时间驱动**（`Math.floor(t * fps) % count`），不用帧计数器——避免暂停/倍速后错位
- 攻击用**帧计数器** `animFrame`（必须精确落在判定帧上）

### 3.4 生成节拍

```js
G.spawnT -= sdt; if (G.spawnT <= 0) { spawnWave(); G.spawnT = BC.spawnInterval; }  // 1.0s
```

- 场上存活 **上限 14**（`BC.maxAlive`），满了就跳过本次生成 —— 防止无限堆积
- **排队机制**：每帧按 x 排序，靠前的先站位 `p.x + e.atkRange`，后面的依次后挪
  `BC.queueGap = 34`px。否则所有怪都会挤在同一点**完全重叠**

---

## 四、伤害与判定

### 4.1 玩家攻击：双段分帧判定

一次挥剑动画（40 帧）触发**两次**独立判定：

| 段 | 触发帧 | 攻击系数 |
|---|---|---|
| 第一段（主刺） | `10` | `0.9~1.1` |
| 第二段（补刺） | `26` | `0.95~1.2` |

**死亡中断**：第一段把目标打死了 → 立即中断，不刺第二下（网游判定，目标已死不补刀）。
攻击动画播放期间**不可被新目标打断**。

### 4.1.1 数值伤害公式（2026-09-15 重建）

玩家的 `atk` **不再硬编码**，由主游戏 `pushBattleStats()` 注入最终面板值（`BattleAPI.setStats`）：

```js
// 主游戏: (裸身 + 装备) × (1 + 词条%)
const s = finalStats({ hp:100+330*lv, atk:10+46*lv, def:5+26*lv }, 装备数值, 装备词条);
BattleAPI.setStats({ ...s, lv });
// 战斗内: 减伤系数 K=100, 破甲按百分比削防御
function calcDmg(atk, mult, def, pen) {
  const eff = Math.max(0, def * (1 - Math.min(90, pen)/100));
  return Math.max(1, Math.round(atk * mult * (100/(100+eff))));
}
```

一次命中的倍率 `mult` 由下列因素**连乘**得到（顺序固定）：

```
基础段系数 → 斩杀(残血 ×2) → 会心/暴击 → 爆伤增幅
   会心 ×1.5（critB%）／暴击 ×2.0（crit%）／爆伤 ×(1 + critD%)
```

> 攻防血**只来自境界与装备**；技能的加成只作用于"打出去的那一下"，不回写面板。

### 4.1.2 怪物成长系统

两种怪的三围随**玩家境界 `lv`** 线性成长（怪只吃境界，**不吃装备** → 换装即提速）：

```js
e.maxHp = Math.round((60 + 26*lv) * def.hpK);
e.atk   = Math.round((8  + 5*lv)  * def.atkK);
e.def   = Math.round((2  + 2*lv)  * def.defK);
```

| 类型 | 名称 | 权重 | `hpK` | `atkK` | `defK` | 速度 | 定位 |
|---|---|---|---|---|---|---|---|
| `slime` | 妖卒 | 60 | 1.0 | 0.55 | 0.35 | 120 | 脆、快，**约一轮两剑收掉** |
| `water` | 水灵 | 40 | 1.6 | 0.75 | 0.60 | 80 | 略厚、慢 |

**精英怪**（`eliteChance 6%`）：体型 ×1.28、血量 ×3、攻击 ×1.2、移速 ×0.85、
产出 ×4，脚下画金色法环。

> 占位怪 `demon`(妖将) / `raptor`(妖弓) 已于本次移除 —— 它们没有真实素材，只画椭圆。
> 当前只有两种有素材的怪。

### 4.2 攻击间隔

```js
p.atkT = 1 / p.aspd;   // 冷却 = 1/攻速
```

| 实体 | `aspd` | 间隔 |
|---|---|---|
| 玩家 | `1.1` | ~0.91s |
| 宠物（默认） | `1.0` | 1.0s |
| 敌人 | `0.8 + random*0.5` | 0.8~1.3 次/秒（**随机化，避免齐攻**） |

### 4.3 敌人攻击

**现状：起手即结算**（无分帧出手动画判定）。

```js
if (Math.abs(e.x - p.x) <= e.atkRange + 5 && e.atkT <= 0) {
  e.anim = 1; e.atkT = 1/(0.8 + Math.random()*0.5);
  // 先判闪避(装备词条 + 身法时效加成), 再走同一套减伤公式
  if (Math.random()*100 < PST.dodge + G.speedDodge) { /* 落空, 飘一个「闪」 */ }
  else { p.hp -= calcDmg(e.atk, 0.85 + Math.random()*0.3, PST.def, 0); p.hurtT = 0.25; p.stun = 0.5; }
  if (p.hp <= 0) p.hp = p.maxHp;                 // 收草节奏: 玩家不死
}
```

| 点 | 现状 |
|---|---|
| 冷却 | `atkT` 独立计时；`anim` 仅控制攻击动画显示（1 → 0，速率 `dt*1.5`） |
| 攻击中可移动 | 是（与玩家相反），保证"边追边打"的压迫感 |
| 受击反馈 | `hurtT = 0.25`s 白闪 + 亮度提升；`stun = 0.5`s 玩家僵直 |

### 4.4 索敌

```js
function findNearestEnemy(fromX, maxDist) { /* 跳过死亡/正在死亡的敌人 */ }
```

- 只看**水平距离**，不看垂直
- **死怪不可选中**：`if (!e.alive || e.dying > 0) continue;`
- `dying` = 已 `alive=false` 但仍在场上播消散（0.4s 淡出），此间不可被选中
- 索敌半径 `atkRange + 200`——**视野 > 攻击范围**，够不着时先走过去

### 4.5 神通技能系统（8 个 · 纯战斗行为）

**定调**：技能**不做常驻属性光环**（不永久加攻/加血/加暴击/减伤），
但**打出去的那一下**完全可以有攻击倍率、会心、暴击、破甲；**身法类在生效期间加闪避**。
属性成长一律来自境界三围 + 装备，两者互不污染。

| # | 技能 | 时机 | 效果 | Lv1 → Lv20 |
|---|---|---|---|---|
| 1 | 剑气斩 `jianqi` | 普攻命中 | 追加一段剑气 | 5% / ×60% → 30% / ×160% |
| 2 | 三连斩 `sanlian` | 第二段后 | 补一刀，**必会心** | 4% / ×50% → 25% / ×130% |
| 3 | 横扫千军 `hengsao` | 普攻命中 | 波及 N 个 | 3% / 2 个 / ×40% → 20% / 5 个 / ×80% |
| 4 | 斩杀 `zhansha` | 命中残血 | 这一击伤害翻倍 | 残血 5% → 25% |
| 5 | 疾风步 `jifeng` | 击杀后 | 2 倍速 + 闪避 | 5%/4s/+4% → 65%/8s/+25% |
| 6 | 缩地成寸 `suodi` | 击杀后 | 3 倍速 + 闪避 | 0.5%/2s/+8% → 12%/4s/+40% |
| 7 | 破甲击 `pojia` | 普攻命中 | 无视目标防御 | 5% / 30% → 25% / 70% |
| 8 | 追猎 `zhuilie` | 击杀后 | 立刻再出手，暴击加成 | 5% / +20% → 30% / +100% |

- **数值**：`from(Lv1) → to(Lv20)` 线性插值，调参只改这两组数（`game.js` 的 `SKILL_DEFS`）
- **成长**：`expNeed(lv) = round(30 * lv^1.35)`，20 级封顶；
  触发/生效 +2~3 exp，每次击杀**全部技能 +2**（不触发也在慢慢长）
- **存档**：`state.skills = { id: {lv, exp} }`，已进 `G1_TPL`，老档惰性初始化
- **面板**：入口是 HUD 的「技」chip → `#skillModal`

### 4.6 战斗产出（只掉灵石 + 法宝）

战斗**不再产修为** —— 修为全部走聚灵阵 `rateNow()`。击杀产出：

```js
// 战斗内(灵石): 服务端系数 × 随机浮动 × 精英倍率
sp = round((DROP.spiritBase + DROP.spiritPerLv*lv) * (0.6~1.4) * (elite ? 4 : 1));
BattleAPI.onDrop({ spirit: sp, elite, enemy });   // 回调主游戏
// 主游戏侧: 灵石入账 + 按 DROP_CFG.equipChance 掷点 → makeArt() → keepArtQuiet() 静默择优
```

| 系数 | 默认 | 说明 |
|---|---|---|
| `spiritBase` / `spiritPerLv` | 6 / 1.2 | 每杀灵石 = 基数 + 每级加成 |
| `spiritRand` | 0.4 | ±40% 浮动 |
| `equipChance` | 3.5% | 掉法宝概率（精英 ×4） |
| `eliteChance` / `eliteMul` / `eliteHp` | 6% / 4 / 3 | 精英怪三件套 |

**结算权威**：服务端下发「每击杀产出」系数 → `applyDropRates(j.dropRates)` →
本地按击杀实时结算；拿不到就用内置默认值，**断网照常可玩**。

> 文字巡猎（`fireFight`/`fireEvent`/`tryStrike`）已整段注释留档，
> `offlineFightWin` 调空 stub 的 TypeError 隐患一并消除。

---

## 五、对外接口 `window.BattleAPI`（唯一耦合点）

| 方法 | 签名 | 作用 |
|---|---|---|
| `getKills()` | `→ number` | 当前击杀数 |
| `resetKills()` | `→ void` | 清零（换境界/重置时调用） |
| `triggerSpeedSkill(mult, dur)` | `→ bool` | 直接触发倍速（`mult<=1` 返回 false） |
| `trySpeedSkill()` | `→ {name,mult,duration}\|null` | 按身法技能概率随机触发 |
| `getSpeedMult()` | `→ number` | 当前倍速 |
| **`setStats(s)`** | `← {lv,atk,hp,def,crit,critB,critD,pen,dodge}` | **主游戏注入面板三围**（装备/境界变化时调用） |
| `getStats()` | `→ object` | 当前注入值快照 |
| **`setDropRates(r)`** | `← object` | **服务端下发掉落系数** |
| `getDropRates()` | `→ object` | 当前掉落系数 |
| **`onDrop`** | `← ({spirit, elite, enemy}) => void` | **击杀回流回调**，由主游戏赋值 |
| `getSpirit()` | `→ number` | 战斗累计产出灵石 |
| `debug()` | `→ {enemies[], playerX, dodge, nextCrit}` | 调参用只读快照 |
| `addPet(def)` / `removePet(id)` / `getPets()` | — | 宠物挂载/卸载/查询（**预留，未实现实体**） |
| `spawnWave()` | `→ void` | 立即刷一波（受 `maxAlive` 限制） |
| `pause()` / `resume()` | `→ void` | 暂停/恢复（**主游戏弹窗时必须 pause**，见 §7.2） |
| `getState()` | `→ {kills,spirit,speedMult,state,playerHp,pets,enemies}` | 状态快照 |

> 战斗 IIFE **不读 `window.state`**，只通过 `BattleAPI` 双向通信；
> 主游戏侧对应 `pushBattleStats()` / `onBattleDrop()` / `bindBattleHooks()` / `window.SkillAPI`。

### 5.1 宠物定义 schema

```js
BattleAPI.addPet({
  id, name,
  atk: 5, aspd: 1.0, atkRange: 50, hp: 80,
  color: '#ffd76b',
  offsetX: -30, offsetY: 0,     // 相对玩家的偏移（负值 = 在玩家身后）
});
```

> 宠物是**预留接口**，默认不挂载。跟随玩家（阻尼 `dt*8`），自动攻击最近敌人。

---

## 六、素材现状（**记录，非强制**）

> 这一节只描述"当前素材长什么样"，**不构成接入新素材的强制流程**。
> 美术怎么出图、用什么流水线，由素材侧自己定；本文档不规定。

| 素材 | 规格 | 帧 |
|---|---|---|
| `cultivator_sheet.webp` | 240×128/帧，9 列 | walk 32（0–31）、attack 40（32–71） |
| `monster_001_green_slime.webp` | 240×128/帧，12 列 | walk 32（0–31）、attack 51（32–82）、hurt 11（83–93） |
| `monster_002_water_sprite.webp` | 240×128/帧，10 列 | walk 32（0–31）、attack 33（32–64）、hurt 13（65–77） |

贴图已于 2026-09-14 全量转 **WebP**（体积 −30%），PNG 已删除。

**绘制现状**：
- 玩家与怪物分别走各自的绘制分支，尺寸为固定值（`min(CH*0.5, 72)` / `min(CH*0.5, 70)`）
- 缩放按**整帧等比**（`drawW = drawH * fw/fh`），**不按内容框逐帧缩放**
- 脚底对齐用常量偏移（史莱姆 120 / 水精灵 118，按 `drawH/fh` 换算）
- 怪物有轻微形态校正：`scaleY 0.92` 压平底部、水精灵额外 `skewX -0.03` 校正倾斜
- 素材未就绪时降级为占位图形，保证战斗画面永远能画出来

> **已知问题**：这两张怪物素材本身生成得有问题（形态不准），所以才需要上面的校正。
> 素材重新生成后，校正幅度预期会大幅减小。

---

## 七、血泪教训

### 7.1 攻击距离与视觉不符

**症状**：刀尖明明碰到敌人却不掉血，或隔空打牛。
**根因**：`atkRange` 是世界单位，与 sprite 尺寸无关；美术改了武器长度却没同步数值。
**教训**：改素材的视觉尺寸后，回头对一次 `atkRange`。

### 7.2 弹窗点不动（z-index 血案）

**症状**：弹窗打开后所有交互失效。
**根因**：战斗层 `.battle-stage` 的 `z-index` 高于 UI 层，铺满全屏的透明层吃掉了点击。
**规则**：`.battle-stage` 必须保持 `z-index: 1`，**低于 UI 最低层 5**。

### 7.3 角色抽搐/横跳

**症状**：走路时角色左右/上下抖动。
**根因**：每帧按独立 bbox 裁剪，框随姿态变 → 落点抖动。
**教训**：**不要按逐帧内容框决定缩放**。缩放应该整帧等比、固定不变；
逐帧内容框只适合用来做对齐参考，一旦拿来算 `k = hitH / box.h`，
蜷缩帧就会被拉高，表现为"拉伸得不成样子"（2026-09-15 实测，已回退）。

### 7.4 走路循环不流畅

**症状**：循环接缝处"顿一下"。
**根因**：首尾帧不是同一姿态。
**教训**：walk 段首尾必须能接上；`monster_002` 用的是"拉伸帧 + 倒放帧"拼出的回环，属于合法做法。

### 7.5 `NaN` 帧号

**症状**：角色**随机几帧消失**。
**根因**：`Math.floor(NaN) % n` 仍是 `NaN`，`drawImage` **静默失败**（不报错、不画）。
**教训**：帧号进 `drawImage` 前用 `Number.isFinite` 兜底。

---

## 八、代码规范

### 8.1 结构

战斗系统整体是 `index.html` 内的一个 IIFE，内部分层：

```
常量 BC → 状态 G → update*（逻辑）→ draw*（渲染）→ 对外 BattleAPI
```

### 8.2 命名约定

| 前缀 | 含义 |
|---|---|
| `BC.*` | 平衡与配置常量 |
| `G.*` | 运行时状态 |
| `update*` | 逻辑推进（受 `sdt` 影响） |
| `draw*` | 渲染（不得修改状态） |

### 8.3 数值调整纪律

- 平衡数值集中放 `BC`，避免在逻辑里散落
- 改判定手感时，先确认改的是"起手容差"还是"命中容差"（§2.3）

### 8.4 渲染规范

- `draw*` 函数**只读状态**，不做逻辑推进
- 所有角色绘制都以 `floorY()` 为脚底基准
- 变换（`translate` / `scale`）必须成对 `save()` / `restore()`

---

## 九、性能预算

| 项 | 预算 |
|---|---|
| 同屏敌人数 | ≤ 20（超出不再生成） |
| 单帧 `drawImage` | ≤ 60 次 |
| 特效粒子 | `G.fx` 生命周期结束即过滤，不驻留 |
| 飘字 | 0.9s 后过滤 |
| 掉帧保护 | `dt` 上限 0.05s |

---

## 十、测试清单（改动后必跑）

| # | 检查 | 方法 |
|---|---|---|
| 1 | 页面无 JS 报错 | 控制台 clean |
| 2 | 玩家持续推进、自动攻击 | 跑 30s |
| 3 | 敌人从右侧生成并接近 | 跑 30s |
| 4 | 击杀数持续增长 | `BattleAPI.getKills()` |
| 5 | 玩家会被扣血但不会真死 | `getState().playerHp` |
| 6 | 怪物形态**没有拉伸/抖动** | **肉眼看**——这条自动化测不出来，别省 |
| 7 | 弹窗打开后仍能点击 | 开任意弹窗 |
| 8 | 切后台再回来不瞬移 | 切走 10s 再回来 |
| 9 | 三围注入正确 | `BattleAPI.getStats()` 与主游戏面板一致 |
| 10 | 换高属性装备后打得更快 | 换装前后各测 20s 击杀数（实测 16 → 31） |
| 11 | 场上怪 ≤ 14 且不重叠 | `BattleAPI.debug()` 看 x 间距 ≥ 30 |
| 12 | 只有 slime/water 两种怪 | `debug().enemies` 的 `t` 去重 |
| 13 | 技能经验会涨、会升级 | 跑 25s 看 `SkillAPI.lv/exp` |
| 14 | 身法加速 + 闪避生效 | 拉满技能后 `getState().speedMult > 1`、`debug().dodge > 0` |
| 15 | 击杀有产出 | `getState().spirit` 增长、法宝偶尔入账 |

---

## 十一、待办与已知缺口

| # | 事项 | 优先级 |
|---|---|---|
| 1 | 重新生成两张怪物素材（当前形态不准，依赖 scaleY/skew 校正掩盖） | **高** |
| 2 | **扩容怪物种类**：当前只有 `slime`/`water` 两种（占位怪已删），出新素材即可按 `hpK/atkK/defK` 接入 | 中 |
| 3 | **宠物系统**：`addPet/removePet/getPets` 接口已预留，缺实体 sprite 与属性来源 | 中 |
| 4 | 服务端 `/api` 真正下发 `dropRates`（当前走内置默认值，接口与本地降级已就绪） | 中 |
| 5 | 战斗产出与服务端对账（现在只做"本地实时结算"，尚未周期对账） | 低 |
| 6 | 敌人是否改成分帧出手（当前起手即结算，无"抬手中被打断"的表现） | 低 |

---

## 十二、文件清单与工作流

| 文件 | 作用 |
|---|---|
| `index.html` | 战斗系统全部实现（内联 IIFE） |
| `assets/cultivator_sheet.webp` | 玩家 sprite |
| `assets/monster_001_green_slime.webp` / `meta.json` | 史莱姆素材 |
| `assets/monster_002_water_sprite.webp` / `meta.json` | 水精灵素材 |
| `tools/build_sprite_sheet.py` | sprite 生成流水线 |
| `tools/ota-manifest.py` | OTA 资源清单生成 |
| `docs/combat-system.md` | 本文档 |

### 12.1 改动工作流

```bash
# 1) 先拉：本仓库有并发智能体 + CI 机器人在提交
git pull --rebase

# 2) 改代码

# 3) 语法自检
node --check <(提取的内联 script)

# 4) 起独立验证服务器（禁止占用 /workspace，禁止 pkill）
python3 -m http.server 8091

# 5) 肉眼确认形态（尤其"有没有被拉伸"），再推
git push
```

> **铁律**：`git pull` 永远在 `git push` 之前。本仓库有并发智能体和 CI 机器人共享同一 remote。

---

## 附录 A：关键常数速查

```js
// —— 平衡（BC）——
playerAtkRange: 75    playerAspd: 1.1      playerSpeed: 28
spawnInterval: 1.0    enemySpawnOffset: 40    maxAlive: 14    queueGap: 34
（playerAtk/playerHp 已废 —— 三围由主游戏 setStats 注入）

// —— 判定容差（现状为裸数字）——
起手 +5    命中 +15    索敌 +200（宠物 +100）    停步 +2
受击白闪 0.25s    死亡消散 0.4s    玩家僵直 0.5s

// —— 敌人(三围 = 系数 × 玩家境界, 见 §4.1.2) ——
slime:  atkRange 30  speed 120  w 60  hpK 1.0  atkK 0.55  defK 0.35
water:  atkRange 35  speed  80  w 40  hpK 1.6  atkK 0.75  defK 0.60
精英:   6% 出现   体型 ×1.28   血 ×3   攻 ×1.2   产出 ×4

// —— 玩家攻击帧 ——
第一段: frame 10  系数 0.9~1.1
第二段: frame 26  系数 0.95~1.2
（暴击/会心/爆伤一律读 PST.crit / critB / critD）

// —— 伤害公式 ——
dmg = atk × mult × 100/(100 + def×(1-pen%)),  最低 1

// —— 摄像机 ——
主角锚点 28%   阻尼 dt*6

// —— 时间 ——
dt 上限 0.05s   动画 fps 24   逻辑用 sdt / 动画用 dt

// —— 倍速(身法技能, 见 §4.5) ——
疾风步  2x：Lv1 5%/4s/+4%闪避   → Lv20 65%/8s/+25%
缩地成寸 3x：Lv1 0.5%/2s/+8%闪避 → Lv20 12%/4s/+40%

// —— 掉落(见 §4.6) ——
spiritBase 6   spiritPerLv 1.2   spiritRand ±40%
equipChance 3.5%   精英 ×4

// —— z-index ——
battle-stage: 1（必须低于 UI 最低层 5）
```
